server:
  port: 8082

spring:
  application:
    name: song-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://hpdariteriiii:Hp31032004@repparton.82qvi0m.mongodb.net/repparton_songs?retryWrites=true&w=majority&appName=Repparton}
  servlet:
    multipart:
      max-file-size: ${UPLOAD_MAX_FILE_SIZE:100MB}
      max-request-size: ${UPLOAD_MAX_REQUEST_SIZE:100MB}
  cache:
    type: redis
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    timeout: ${REDIS_TIMEOUT:2000ms}
    database: ${REDIS_DATABASE:0}

management:
  endpoints:
    web:
      exposure:
        include: health,info,caches

logging:
  level:
    com.DA2.songservice: DEBUG

# AI Service Configuration
ai:
  service:
    url: ${AI_SERVICE_URL:http://localhost:5000}
